export enum UserType {
  SHOPPER = 'shopper',
  SELLER = 'seller',
}

export enum Screen {
  HOME = 'home',
  LOGIN = 'login',
  SIGNUP = 'signup',
  EVENTS = 'events',
  CART = 'cart',
  CHECKOUT = 'checkout',
}
